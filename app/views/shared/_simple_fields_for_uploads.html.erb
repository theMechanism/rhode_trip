<% if resource.uploads.empty? %>
  <% uploads = [ resource.uploads.build ] %>
<% else %>
  <% uploads = resource.uploads << resource.uploads.build %>
<% end %>

<%= f.fields_for :uploads do |u| %>
  <!-- < % uploads.each do |up| %> -->

    <% label = u.object.id ? 'Change attachment' : 'Add attachment' %>
    <%= u.file_field :attachment %>
  <!-- < % end %> -->
<% end %>